'use strict';

const ROOT = '/analytics';

const register = ({ expressApp, config, logger, dbPool }) => {
    // const eventsRoute = require('./')({ config, logger, dbPool });
    // expressApp.use(ROOT, eventsRoute);
};

module.exports = {
    register
};